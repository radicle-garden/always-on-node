repos:
  - repo: local
    hooks:
      - id: pnpm-check
        name: pnpm check
        entry: pnpm check
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true
